<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.nhn.school.dao.TeacherDao">

	<select id="findAll" resultType="com.nhn.school.model.Teacher">
		/* teacherDao.findAll */
		SELECT id, name, birth, subId
		FROM teacher
	</select>
	
	<insert id="save">
		<choose>
			<when test="id != null and id != 0">
				INSERT INTO teacher
				(id, name, birth, subId)
				VALUES
				(#{id}, #{name}, #{birth}, #{subId})
				ON DUPLICATE KEY UPDATE name=#{name}
				, birth=#{birth}
				, subId=#{subId}
			</when>
			<otherwise>
				INSERT INTO teacher
				(name, birth, subId)
				VALUES
				(#{name}, #{birth}, #{subId})
			</otherwise>
		</choose>
	</insert>
	
	<delete id="delete">
		DELETE FROM teacher
		WHERE id=#{id}
	</delete>
</mapper>
