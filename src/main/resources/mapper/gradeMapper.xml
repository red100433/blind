<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.nhn.school.dao.GradeDao">

	<select id="findAll" resultType="com.nhn.school.model.Grade">
		/* gradeDao.findAll */
		SELECT * FROM grade
	</select>
	
		<insert id="save">
		<choose>
			<when test="id != null and id != 0">
				INSERT INTO grade
				(id, stuId, subId, score)
				VALUES
				(#{id}, #{stuId}, #{subId}, #{score})
				ON DUPLICATE KEY UPDATE stuId=#{stuId}
				, subId=#{subId}
				, score=#{score}
			</when>
			<otherwise>
				INSERT INTO grade
				(stuId, subId, score)
				VALUES
				(#{stuId}, #{subId}, #{score})
			</otherwise>
		</choose>
	</insert>
	
	<delete id="delete">
		DELETE FROM grade
		WHERE id=#{id}
	</delete>
</mapper>
